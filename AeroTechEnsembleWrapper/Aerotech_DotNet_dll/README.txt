
1) Two ways of utilising the dynamic link library (*.dll) files provided by Thorlabs:

    _> Copy *.dll file needed (e.g. AeroBasic) to this folder from the software location of 'Ensemble'

    _> Provide a libraryPath to the dll files.

2) Currently, the dll library used is: Aerotech.Ensemble.dll